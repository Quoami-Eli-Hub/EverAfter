"use client";
import {useState} from "react";
export function DemoRsvpForm(){const[done,setDone]=useState(false);if(done)return <div className="rsvp-success">Demo RSVP received. Live pages save this for the owner.</div>;return <form onSubmit={e=>{e.preventDefault();setDone(true)}}><input aria-label="Full name" placeholder="Your full name" required/><input aria-label="Phone number" placeholder="Phone number" required/><select aria-label="Attendance" defaultValue="" required><option value="" disabled>Will you attend?</option><option>Joyfully accepts</option><option>Regretfully declines</option></select><select aria-label="Additional guests" defaultValue="0"><option value="0">No additional guests</option><option>1 additional guest</option><option>2 additional guests</option></select><button>Send my RSVP</button></form>}
export function DemoTributeForm(){const[done,setDone]=useState(false);if(done)return <div className="rsvp-success">Demo tribute submitted for family approval.</div>;return <form onSubmit={e=>{e.preventDefault();setDone(true)}}><input aria-label="Full name" placeholder="Your full name" required/><textarea aria-label="Tribute" placeholder="Your memory or message" required minLength={10}/><button>Submit for review</button></form>}
export function DemoDownloadButton(){function download(){const blob=new Blob(["EVERAFTER DEMO\n\nSample funeral programme\nOwner-controlled document download demonstration."],{type:"text/plain"});const url=URL.createObjectURL(blob);const link=document.createElement("a");link.href=url;link.download="sample-funeral-programme.txt";link.click();URL.revokeObjectURL(url)}return <button type="button" onClick={download}>Download ↓</button>}
