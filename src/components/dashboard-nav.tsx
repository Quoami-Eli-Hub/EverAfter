import Link from "next/link";
import {signOut} from "@/app/auth/actions";
export function DashboardNav({active}:{active:string}){const links=[["Events","/dashboard/events"],["Overview","/dashboard"],["Editor","/dashboard/editor"],["Schedule","/dashboard/schedule"],["Guests","/dashboard/guests"],["Gallery","/dashboard/gallery"],["Messages","/dashboard/tributes"],["Team","/dashboard/team"],["Settings","/dashboard/settings"],["Plan","/dashboard/billing"]];return <header className="app-header"><Link className="brand" href="/"><span>ev.</span> EverAfter</Link><nav className="dash-nav">{links.map(([label,href])=><Link className={active===label?"active":""} key={href} href={href}>{label}</Link>)}</nav><form action={signOut}><button className="logout-button">Sign out</button></form></header>}
