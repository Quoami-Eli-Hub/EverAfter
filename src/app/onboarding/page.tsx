import Link from "next/link";
import {createEvent} from "./actions";
import {requireUser} from "@/lib/auth";

export default async function Onboarding({searchParams}:{searchParams:Promise<{message?:string}>}){
  await requireUser();const{message}=await searchParams;
  return <div className="onboard-page"><header className="app-header"><Link className="brand" href="/"><span>ev.</span> EverAfter</Link><span className="muted">Create an event · One account, every workspace</span><Link href="/dashboard/events">Exit setup</Link></header><main className="onboard-wrap"><div className="progress"><i/><i/><i/><i/></div><p className="eyebrow">Add a workspace</p><h1>What are we gathering for?</h1><p className="onboard-lead">Choose the occasion and EverAfter will prepare the right sections. Create as many client workspaces as you need and manage them from one account.</p>{message&&<div className="auth-message">{message}</div>}<form action={createEvent}><div className="type-grid"><label className="type-card"><input className="type-radio" type="radio" name="eventType" value="wedding" defaultChecked/><span className="type-art wedding-mini">A <i>&amp;</i> K</span><b>Wedding</b><small>Joyful, warm and celebratory</small></label><label className="type-card"><input className="type-radio" type="radio" name="eventType" value="memorial"/><span className="type-art memorial-mini">KM</span><b>Funeral or memorial</b><small>Calm, dignified and reflective</small></label></div><div className="form-preview panel onboarding-fields"><label>Event page title<input name="title" placeholder="Ama & Kojo or Remembering Kofi Mensah" required minLength={3}/></label><label>Event date<input name="eventDate" type="date"/></label><label>Your preferred page address<div className="slug-field"><span>everafter.com/</span><input name="slug" placeholder="ama-and-kojo" required pattern="[a-z0-9]+(?:-[a-z0-9]+)*"/><b>Unique</b></div></label><small>Use lowercase letters, numbers and hyphens. Availability is confirmed when you continue.</small></div><div className="onboard-actions"><Link href="/dashboard/events" className="text-link">Back to events</Link><button className="button button-coral" type="submit">Create event workspace →</button></div></form></main></div>;
}
