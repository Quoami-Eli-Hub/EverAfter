import Link from "next/link";
import {updatePassword} from "@/app/auth/actions";
export default async function Reset({searchParams}:{searchParams:Promise<{message?:string;tone?:string}>}){const{message,tone}=await searchParams;return <main className="auth-page"><section className="auth-aside"><Link className="brand" href="/"><span>ev.</span> EverAfter</Link><div><p className="eyebrow">Secure account</p><h1>Choose a new password.</h1><p>A unique password helps keep every event and guest list protected.</p></div></section><section className="auth-form-wrap"><div className="auth-form"><h2>New password</h2><p>Use at least 12 characters and avoid a password you use elsewhere.</p>{message&&<div className={tone==="success"?"rsvp-success":"auth-message"} role={tone==="success"?"status":"alert"}>{message}</div>}<form action={updatePassword} className="auth-fields"><label>New password<input name="password" type="password" autoComplete="new-password" minLength={12} required/></label><label>Confirm password<input name="confirm" type="password" autoComplete="new-password" minLength={12} required/></label><button className="button button-coral full">Update password</button></form></div></section></main>}
