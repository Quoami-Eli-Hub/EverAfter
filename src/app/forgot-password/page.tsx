import Link from "next/link";
import {requestPasswordReset} from "@/app/auth/actions";
export default async function Forgot({searchParams}:{searchParams:Promise<{message?:string;tone?:string}>}){const{message,tone}=await searchParams;return <main className="auth-page"><section className="auth-aside"><Link className="brand" href="/"><span>ev.</span> EverAfter</Link><div><p className="eyebrow">Account recovery</p><h1>Return to your workspace.</h1><p>We will help you regain secure access without exposing whether an email is registered.</p></div></section><section className="auth-form-wrap"><div className="auth-form"><h2>Reset your password</h2><p>We will email a secure, time-limited recovery link.</p>{message&&<div className={tone==="success"?"rsvp-success":"auth-message"} role={tone==="success"?"status":"alert"}>{message}</div>}<form action={requestPasswordReset} className="auth-fields"><label>Email address<input name="email" type="email" autoComplete="email" required/></label><button className="button button-coral full">Send reset link</button></form><p className="auth-switch"><Link href="/login">Back to sign in</Link></p></div></section></main>}
